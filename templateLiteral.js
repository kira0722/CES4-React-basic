const pais = "españa";
const capital = "Madrid";
const divisa = "euro";

const mensaje = "el pais " + pais + " con la capital "+ capital + " la divisa es: "+ divisa;

console.log(mensaje);

const literal = `el pais de ${pais} con la capital ${capital} la divisa es ${divisa}`;

console.log(literal);